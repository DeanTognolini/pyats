# =============================================================================
# pyATS Environment Variables Template
# =============================================================================
#
# Copy this file to .env and fill in your actual credentials:
#   cp .env.example .env
#
# Then load the variables before running tests:
#   export $(cat .env | xargs)
#   # OR source the file:
#   set -a; source .env; set +a
#
# SECURITY WARNING: Never commit .env to version control!
# The .env file is already in .gitignore
#
# =============================================================================

# Network Device Credentials
# ---------------------------
# Username for SSH authentication to network devices
PYATS_USERNAME=admin

# Password for SSH authentication to network devices
PYATS_PASSWORD=your-secure-password

# Optional: Enable password (if required by your devices)
# PYATS_ENABLE_PASSWORD=your-enable-password

# Optional: TACACS+ or RADIUS server settings (if using centralized auth)
# PYATS_AUTH_SERVER=tacacs.example.com
# PYATS_AUTH_KEY=your-auth-key

# Logging Configuration
# ---------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
PYATS_LOG_LEVEL=INFO

# Optional: Custom log directory
# PYATS_LOG_DIR=/var/log/pyats

# Optional: Enable detailed Unicon logs
# UNICON_LOG_LEVEL=DEBUG

# Test Configuration
# ------------------
# Optional: Maximum number of parallel workers
# PYATS_MAX_WORKERS=5

# Optional: Connection timeout in seconds
# PYATS_CONNECTION_TIMEOUT=30

# Optional: Command execution timeout in seconds
# PYATS_COMMAND_TIMEOUT=60

# Reporting Configuration
# -----------------------
# Optional: Report output directory
# PYATS_REPORT_DIR=./reports

# Optional: Enable HTML reports (true/false)
# PYATS_HTML_REPORTS=true

# Optional: Enable JSON reports (true/false)
# PYATS_JSON_REPORTS=true

# Docker Configuration
# --------------------
# When running in Docker, these will be passed via -e flags
# See README.md for Docker usage examples
